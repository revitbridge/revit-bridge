{
  "description": "Confirmation gate: execute_code and run_tool are denied unless they carry a token from confirm_spec.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__(plugin_revit-bridge_)?revit-bridge__(execute_code|run_tool)",
        "hooks": [
          {
            "type": "command",
            "command": "uv",
            "args": ["run", "--no-project", "--quiet", "${CLAUDE_PLUGIN_ROOT}/hooks/spec_gate.py"],
            "timeout": 30
          }
        ]
      }
    ]
  }
}
